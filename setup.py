from setuptools import setup

setup(
    name="uqlib",
    version="0.01",
    packages=["uqlib"],
    package_dir={
        "uqlib": "./uqlib",
    },
    install_requires=[
        # List your project dependencies here
    ],
)
